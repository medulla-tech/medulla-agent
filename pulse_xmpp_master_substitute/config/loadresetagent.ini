[parameters]
# Disable processing by resetagent: reset_machine is now processed by 
# plugin_regenerateagent to rebuild image via relay.
reset_queue_scan_enabled = False

# Interval in seconds between each queue scan.
# A lower bound of 10s is enforced by the plugin.
reset_queue_scan_interval = 60
