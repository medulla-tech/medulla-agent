The template files are used to generate on fly the monitoring's scripts.
The template scripts MUST be installed in /var/lib/pulse/script_monitoring

The scripts are generated on fly in the /var/lib/pulse/script_monitoring folder.
